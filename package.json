{
  "name": "tarrot-project-2-validator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:static": "node scripts/check-static.mjs",
    "check": "npm test && npm run check:static"
  },
  "engines": {
    "node": ">=22"
  }
}
